# 着丼 (tyakudon)

ラーメン店の待ち時間を記録・共有するサービス。ユーザーが行列に並び始めてからラーメンが提供されるまでを実測し、その記録を他のユーザーと共有する。

## Language

### 待機の記録

**接続**:
ラーメン店の待ち行列に並び始め、着丼記録を開始すること。`Record#started_at` がセットされる。
_Avoid_: チェックイン, 開始, 並ぶ

**着丼**:
注文したラーメンが提供されること。接続の終点で、`Record#ended_at` がセットされる。
_Avoid_: 完了, ゴール, 提供

**着丼記録**:
接続から着丼までの1回の待機を表す記録 (`Record`)。
_Avoid_: ちゃくどんレコード, レコード, 待ち時間記録

**待ち時間**:
接続から着丼までの経過時間 (`Record#wait_time`)。
_Avoid_: 所要時間, 経過時間

**接続中**:
接続したが着丼もリタイアもしていない状態。`Record#wait_time` が `nil` であることで表される。
_Avoid_: 待機中, 進行中

**行列状況**:
接続中に報告する、その時点の行列の様子 (`LineStatus`)。店内・店外・着席のいずれかと、行列数、コメントを持つ。1つの着丼記録に複数ぶら下がる。
_Avoid_: 待ち行列情報, 行列報告, キュー

**リタイア**:
着丼せずに着丼記録を終えること。ユーザー自身が終える場合 (`Record#is_retired`) と、放置により自動で終わる**自動リタイア** (`Record#auto_retired`) を区別する。
_Avoid_: キャンセル, 中断, 離脱

**テスト記録**:
テストモードのユーザーが作った、集計や一覧に含めない着丼記録 (`Record#is_test`)。
_Avoid_: ダミーレコード, 検証データ

### 店舗とユーザーの関係

**ラーメン店**:
待ち時間を記録する対象の店舗 (`RamenShop`)。住所から Geocoder で緯度経度を持つ。
_Avoid_: 店, ショップ, 飲食店

**お気に入り店舗**:
ユーザーが継続的に追いかけるラーメン店 (`Favorite`)。**店舗そのもの**を指す。
_Avoid_: フォロー, ブックマーク, ウォッチ

**お気に入り店舗の記録**:
お気に入り店舗に対して投稿された着丼記録のまとまり。「お気に入り店舗」（店舗の一覧）と別物なので、UI 上のラベルで必ず区別する。
_Avoid_: お気に入り店舗（記録を指して使わない）, お気に入り記録

**いいね**:
他のユーザーの着丼記録への反応 (`Like`)。
_Avoid_: ファボ, スター, 高評価

**店舗登録申請**:
未登録のラーメン店の追加をユーザーが依頼するもの (`ShopRegisterRequest`)。管理者が承認すると店舗が登録され、申請者に完了メールが届く。
_Avoid_: 店舗リクエスト, 追加申請

### UI の世代

**v1**:
Bootstrap 5 と固定ヘッダーに依存した初期の UI。
_Avoid_: 旧UI, レガシーUI

**v2**:
ブラウザデフォルトを最大限活用するミニマルな UI。詳細は [docs/v2_ui_migration.md](./docs/v2_ui_migration.md)。
_Avoid_: 新UI, シンプルUI
