<% if logged_in? %>
  <% if current_user.favorites?(@ramen_shop) %>
    <%= render 'remove_favorite' %>
  <% else %>
    <%= render 'add_favorite' %>
  <% end %>
<% else %>
  <%= link_to t('.not_logged_in'), prepare_favorite_ramen_shop_path(@ramen_shop), class: 'btn-link' %>
<% end %>
