<% if logged_in? %>
  <% if current_user.favorites?(@ramen_shop) %>
    <%= render 'remove_favorite' %>
  <% else %>
    <%= render 'add_favorite' %>
  <% end %>
<% else %>
  <%= link_to 'お気に入り', prepare_favorite_ramen_shop_path(@ramen_shop), class: 'btn-link' %>
<% end %>
