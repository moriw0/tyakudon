<% favorite = current_user.favorites.find_by(ramen_shop: @ramen_shop) %>
<% if favorite %>
  <%= button_to t('.label'), favorite_path(favorite),
        method: :delete,
        form: { class: "form-inline" },
        class: 'btn-link' %>
<% end %>
