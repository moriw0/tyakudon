<p style="color: green"><%= notice %></p>
<%= render @ramen_shop %>
<div>
  <ul>
    <% @ramen_shop.records.each do |record| %>
      <% if record.created_at && record.wait_time %>
        <li>created_at: <%= record.created_at.getutc.strftime("%F") %> 着丼: <%= format_duration(record.wait_time) %></li>
      <% end%>
    <% end %>
  </ul>
</div>
<div>
  <%= link_to "Back to ramen shops", ramen_shops_path %>
</div>
