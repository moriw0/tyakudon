<% add_breadcrumb t('shared.breadcrumb_home') %>
<hr>

<%= render 'shared/nav' %>
<hr>

<h1><%= t('.heading') %></h1>

<%= render 'records_table', records: @records %>
<%= paginate @records if @records.any? %>
