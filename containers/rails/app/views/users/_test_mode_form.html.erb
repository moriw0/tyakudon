<%= bootstrap_form_with model: user, url: update_test_mode_user_path(user), data: { controller: 'test-mode' }, class: 'mt-2' do |f| %>
  <%= f.check_box :is_test_mode, switch: true, data: { action: "change->test-mode#submitForm" }, label: 'TestMode' %>
<% end%>
